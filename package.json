{
  "name": "restaurant-availability-checker-api",
  "version": "1.0.0",
  "description": "A serverless API to check for table availability across multiple booking platforms.",
  "main": "api/check.js",
  "type": "module",
  "scripts": {
    "postinstall": "npx playwright install webkit"
  },
  "dependencies": {
    "@upstash/redis": "^1.22.0",
    "playwright-core": "^1.44.1"
  },
  "devDependencies": {
    "playwright": "^1.44.1"
  }
}

